# Easypanel / Nixpacks — Prisma 7 e NestJS 11 exigem Node >= 20.19
[variables]
NIXPACKS_NODE_VERSION = "22"

[phases.build]
cmds = ["npx prisma generate", "npm run build"]

[start]
cmd = "npx prisma db push && node dist/src/main.js"
